#+TITLE: GNU Emacs Config
#+AUTHOR: Yeonho Jang
#+DESCRIPTION: My Personal Emacs setting
#+STARTUP: showeverything
#+OPTIONS: toc:2

* Table of Contents :toc:
- [[#important-programs-to-load-first][IMPORTANT PROGRAMS TO LOAD FIRST]]
  - [[#elpaca-package-manager][Elpaca Package Manager]]
  - [[#eldoc][Eldoc]]
  - [[#enable-elpaka][Enable Elpaka]]
  - [[#eldoc-1][Eldoc]]
  - [[#general-keybindings][General Keybindings]]
  - [[#back-파일-및-임시파일-이동][Back 파일 및 임시파일 이동]]
- [[#diminish][Diminish]]
- [[#all-icons][All Icons]]
- [[#fonts][FONTS]]
  - [[#setting-the-font-face][Setting The Font Face]]
  - [[#zooming-in-and-out][Zooming in and out]]
- [[#color-scheme][Color Scheme]]
- [[#gui-setup][GUI setup]]
  - [[#disable-menubar-toolbars-scrollbar][Disable Menubar, toolbars, scrollbar]]
  - [[#display-line-numbers-and-truncated-lines][Display Line numbers and Truncated Lines]]
  - [[#dashboard][Dashboard]]
- [[#org-mode][ORG mode]]
  - [[#toc][TOC]]
  - [[#org-bullet][ORG Bullet]]
  - [[#disable-electric-indent][Disable Electric Indent]]
  - [[#scheduling][Scheduling]]
  - [[#ob-mermaid][ob mermaid]]
  - [[#org-roam][ORG-ROAM]]
- [[#which-key][WHICH KEY]]
- [[#프로그래밍-지원][프로그래밍 지원]]
  - [[#rainbow-mode][Rainbow Mode]]
  - [[#flycheck][FlyCheck]]
  - [[#corfu][Corfu]]
  - [[#ivy][IVY]]
  - [[#git-magit][Git (Magit)]]
  - [[#markdown][Markdown]]
  - [[#hide-show-mode][Hide Show mode]]
  - [[#jsonrpc][JsonRPC]]
  - [[#eglot][Eglot]]
  - [[#paredit][paredit]]
- [[#프로그래밍-언어][프로그래밍 언어]]
  - [[#common-lisp][Common Lisp]]
  - [[#clojure][Clojure]]
  - [[#c][C++]]
  - [[#d-lang][D-lang]]
  - [[#rust--rust-mode][Rust  (RUST mode)]]
  - [[#pascal-fpc][PASCAL (FPC)]]
  - [[#typescript][Typescript]]
  - [[#cider][Cider]]
- [[#기타][기타]]
  - [[#backup-및-임시파일-경로-수정][Backup 및 임시파일 경로 수정]]
  - [[#sudo-edit][Sudo Edit]]
  - [[#exec-path-from-shell][Exec Path from shell]]
  - [[#android-tramp-설정][Android Tramp 설정]]
  - [[#odin][ODIN]]
  - [[#ollama][Ollama]]
  - [[#treesitter][TreeSitter]]
  - [[#format-all][Format All]]
- [[#바이브-코딩][바이브 코딩]]
  - [[#aider][Aider]]
  - [[#gptel][gptel]]
  - [[#gptel-mcp][GPTEL MCP]]
  - [[#gptel-directives][GPTEL directives]]
  - [[#gptel-로-common-lisp-refactoring-직접-요청][GPTEL 로 Common lisp Refactoring 직접 요청]]
  - [[#gptel-tool][GPTEL tool]]
  - [[#gptel-agent][GPTEL-AGENT]]
  - [[#sdd][SDD]]
  - [[#저사양기기][저사양기기]]

* IMPORTANT PROGRAMS TO LOAD FIRST

** Elpaca Package Manager

#+BEGIN_SRC emacs-lisp
  ;; -*- lexical-binding: t; -*-
  (defvar elpaca-installer-version 0.11)
  (defvar elpaca-directory (expand-file-name "elpaca/" user-emacs-directory))
  (defvar elpaca-builds-directory (expand-file-name "builds/" elpaca-directory))
  (defvar elpaca-repos-directory (expand-file-name "repos/" elpaca-directory))
  (defvar elpaca-order '(elpaca :repo "https://github.com/progfolio/elpaca.git"
  			      :ref nil :depth 1 :inherit ignore
  			      :files (:defaults "elpaca-test.el" (:exclude "extensions"))
  			      :build (:not elpaca--activate-package)))
  (let* ((repo  (expand-file-name "elpaca/" elpaca-repos-directory))
         (build (expand-file-name "elpaca/" elpaca-builds-directory))
         (order (cdr elpaca-order))
         (default-directory repo))
    (add-to-list 'load-path (if (file-exists-p build) build repo))
    (unless (file-exists-p repo)
      (make-directory repo t)
      (when (<= emacs-major-version 28) (require 'subr-x))
      (condition-case-unless-debug err
  	(if-let* ((buffer (pop-to-buffer-same-window "*elpaca-bootstrap*"))
  		  ((zerop (apply #'call-process `("git" nil ,buffer t "clone"
  						  ,@(when-let* ((depth (plist-get order :depth)))
  						      (list (format "--depth=%d" depth) "--no-single-branch"))
  						  ,(plist-get order :repo) ,repo))))
  		  ((zerop (call-process "git" nil buffer t "checkout"
  					(or (plist-get order :ref) "--"))))
  		  (emacs (concat invocation-directory invocation-name))
  		  ((zerop (call-process emacs nil buffer nil "-Q" "-L" "." "--batch"
  					"--eval" "(byte-recompile-directory \".\" 0 'force)")))
  		  ((require 'elpaca))
  		  ((elpaca-generate-autoloads "elpaca" repo)))
  	    (progn (message "%s" (buffer-string)) (kill-buffer buffer))
  	  (error "%s" (with-current-buffer buffer (buffer-string))))
        ((error) (warn "%s" err) (delete-directory repo 'recursive))))
    (unless (require 'elpaca-autoloads nil t)
      (require 'elpaca)
      (elpaca-generate-autoloads "elpaca" repo)
      (let ((load-source-file-function nil)) (load "./elpaca-autoloads"))))
  (add-hook 'after-init-hook #'elpaca-process-queues)
  (elpaca `(,@elpaca-order))
#+END_SRC


** Eldoc

#+BEGIN_SRC emacs-lisp
  					;  (unload-feature 'eldoc t)
  					;  (setq custom-delayed-init-variables '())
  					;  (defvar global-eldoc-mode nil)
  					;  (elpaca eldoc
  					;    (require 'eldoc)
  					;    (global-eldoc-mode)
  					;    (provide 'eldoc))
#+END_SRC


** Enable Elpaka

#+BEGIN_SRC emacs-lisp

  ;; Install use-package support
  (elpaca elpaca-use-package
    ;; Enable use-package :ensure support for Elpaca.
    (elpaca-use-package-mode)
    (setq elpaca-use-package-by-default 1))

  (elpaca-wait)

  (use-package emacs :ensure nil :config (setq ring-bell-function #'ignore))

#+END_SRC

** Eldoc

#+BEGIN_SRC emacs-lisp
  (unload-feature 'eldoc t)
  (setq custom-delayed-init-variables '())
  (defvar global-eldoc-mode nil)
  (elpaca eldoc
    (require 'eldoc)
    (global-eldoc-mode)
    (provide 'eldoc))
#+END_SRC


** General Keybindings

#+BEGIN_SRC emacs-lisp

  ;;(use-package general
  ;;  :config
  ;;  )

#+END_SRC

** Back 파일 및 임시파일 이동 
#+BEGIN_SRC emacs-lisp
  (let ((backup-dir "~/tmp/emacs/backups")
        (auto-saves-dir "~/tmp/emacs/auto-saves/"))
    (dolist (dir (list backup-dir auto-saves-dir))
      (when (not (file-directory-p dir))
        (make-directory dir t)))
    (setq backup-directory-alist `(("." . ,backup-dir))
          auto-save-file-name-transforms `((".*" ,auto-saves-dir t))
          auto-save-list-file-prefix (concat auto-saves-dir ".saves-")
          tramp-backup-directory-alist `((".*" . ,backup-dir))
          tramp-auto-save-directory auto-saves-dir))

  (setq backup-by-copying t    ; Don't delink hardlinks
        delete-old-versions t  ; Clean up the backups
        version-control t      ; Use version numbers on backups,
        kept-new-versions 5    ; keep some new versions
        kept-old-versions 2)   ; and some old ones, too

#+END_SRC
* Diminish

#+BEGIN_SRC emacs-lisp
  (use-package diminish
    :ensure t)
#+END_SRC

* All Icons

#+BEGIN_SRC emacs-lisp
  (use-package all-the-icons
    :ensure t
    :if (display-graphic-p))

  (use-package all-the-icons-dired
    :ensure t
    :hook (dired-mode . (lambda () (all-the-icons-dired-mode t))))

#+END_SRC

* FONTS

** Setting The Font Face

#+BEGIN_SRC emacs-lisp

  (set-language-environment "Korean")
  (prefer-coding-system 'utf-8)

  (when (or (eq system-type 'gnu/linux)
  	  (eq system-type 'darwin))
    ;; defult Latin font :face
    (set-face-attribute 'default nil :family "Hack")
    (set-face-attribute 'default nil :height 120)
    (set-fontset-font t 'hangul (font-spec :name "D2Coding")))

  ;; 한글과 영문의 폰트 스케일 맞추기 - orgmode 테이블 깨지지않게하기
  (setq face-font-rescale-alist 
        '((".*hiragino.*" . 1.2)
  	("D2Coding" . 1.2307692307692308)))


#+END_SRC



** Zooming in and out

#+BEGIN_SRC emacs-lisp
  (global-set-key (kbd "C-=") 'text-scale-increase)
  (global-set-key (kbd "C--") 'text-scale-decrease)
  (global-set-key (kbd "<C-wheel-up>") 'text-scale-increase)
  (global-set-key (kbd "<C-wheel-down>") 'text-scale-decrease)

#+END_SRC

* Color Scheme
#+BEGIN_SRC emacs-lisp

  (add-to-list 'custom-theme-load-path "~/doc/emacs/themes")
  ;;(load-theme 'dracula t)
  ;;(load-theme 'nimbus t)
  (load-theme 'zenburn t)

#+END_SRC




* GUI setup

** Disable Menubar, toolbars, scrollbar

#+BEGIN_SRC emacs-lisp

  (menu-bar-mode -1)
  (tool-bar-mode -1)
  (scroll-bar-mode -1)

#+END_SRC

** Display Line numbers and Truncated Lines

#+BEGIN_SRC emacs-lisp

  (global-display-line-numbers-mode 1)
  (global-visual-line-mode t)

#+END_SRC


** Dashboard

#+BEGIN_SRC emacs-lisp

  (use-package dashboard
    :ensure t 
    :init
    (setq initial-buffer-choice 'dashboard-open)
    (setq dashboard-set-heading-icons t)
    (setq dashboard-set-file-icons t)
    (setq dashboard-banner-logo-title "Emacs Is More Than A Text Editor!")
    ;;(setq dashboard-startup-banner 'logo) ;; use standard emacs logo as banner
    ;;(setq dashboard-startup-banner ;; "~/.config/emacs/images/dtmacs-logo.png")  ;; use custom image as banner
    (setq dashboard-center-content nil) ;; set to 't' for centered content
    (setq dashboard-items '((recents . 5)
                            (agenda . 5 )
                            (bookmarks . 3)
                            (projects . 3)
                            (registers . 3)))
    :custom 
    (dashboard-modify-heading-icons '((recents . "file-text")
                                      (bookmarks . "book")))
    :config
    (dashboard-setup-startup-hook))


#+END_SRC

* ORG mode

** TOC

#+BEGIN_SRC emacs-lisp
  (use-package toc-org
    :ensure t
    :commands toc-org-enable
    :init (add-hook 'org-mode-hook 'toc-org-enable))
#+END_SRC

** ORG Bullet

#+BEGIN_SRC emacs-lisp
  (add-hook 'org-mode-hook 'org-indent-mode)
  (use-package org-bullets
    :ensure t)
  (add-hook 'org-mode-hook (lambda () (org-bullets-mode 1)))
#+END_SRC

** Disable Electric Indent

#+BEGIN_SRC emacs-lisp
  (electric-indent-mode -1)
#+END_SRc


** Scheduling
#+BEGIN_SRC emacs-lisp
  (use-package org
    :ensure nil
    :config 
    (setq org-agenda-files (list "~/doc/org/agenda.org" "~/doc/org/game_project.org"))
    :bind (("C-c l" . org-store-link)
           ("C-c a" . org-agenda)))

#+END_SRC

** ob mermaid 
#+BEGIN_SRC emacs-lisp
  (use-package ob-mermaid
    :ensure t
    :config
    (setq ob-mermaid-cli-path "~/.local/bin/mmdc")
    (org-babel-do-load-languages
     'org-babel-load-languages
     '((mermaid . t)
       (scheme . t)
       (lisp . t)
       (emacs-lisp . t))))
#+END_SRC

** ORG-ROAM

#+BEGIN_SRC emacs-lisp
  (use-package org-roam
    :ensure t
    :init
    (setq org-roam-v2-ack t)
    (setq ob-mermaid-cli-path "/usr/local/bin/mmdc")

    :custom 
    (org-roam-directory "~/doc/org-roam")
    (org-roam-completion-everywhere t)
    :bind (("C-c n l" . org-roam-buffer-toggle)
           ("C-c n f" . org-roam-node-find)
           ("C-c n i" . org-roam-node-insert)
           :map org-mode-map
           ("C-M-i" . completion-at-point)
           :map org-roam-dailies-map
           ("Y" . org-roam-dailies-capture-yesterday)
           ("T" . org-roam-dailies-capture-tomorrow))
    :bind-keymap
    ("C-c n d" . org-roam-dailies-map)
    :config
    (require 'org-roam-dailies)
    (org-roam-db-autosync-mode))
#+END_SRC

* WHICH KEY

#+BEGIN_SRC emacs-lisp

  (use-package which-key
    :ensure t
    :init
    (which-key-mode 1)
    :config
    (setq which-key-side-window-location 'bottom
  	which-key-sort-order #'which-key-key-order-alpha
  	which-key-sort-uppercase-first nil
  	which-key-add-column-padding 1
  	which-key-max-display-columns nil
  	which-key-min-display-lines 6
  	which-key-side-window-slot -10
  	which-key-side-window-max-height 0.25
  	which-key-idle-delay 0.8
  	which-key-max-description-length 25
  	which-key-allow-imprecise-window-fit t
  	which-key-separator " → " ))

#+END_SRC


* 프로그래밍 지원

** Rainbow Mode

#+BEGIN_SRC emacs-lisp
  (use-package rainbow-mode
    :ensure t
    :hook 
    ((org-mode prog-mode) . rainbow-mode))
#+END_SRC

** FlyCheck

#+BEGIN_SRC emacs-lisp
  (use-package flycheck
    :ensure t
    :defer t
    :diminish
    :init (global-flycheck-mode))

  (use-package flycheck-dmd-dub
    :after (flycheck d-mode)
    :ensure 
    ( :package "flycheck-dmd-dub"
      :repo "atilaneves/flycheck-dmd-dub"
      :fetcher github
      :source "MELPA")
    :hook
    (d-mode . flycheck-dmd-dub-set-variables)
    )

  (use-package flycheck-eglot
    :ensure t
    :after (flycheck eglot)
    :config
    (global-flycheck-eglot-mode 1))

#+END_SRC

** Corfu


#+BEGIN_SRC emacs-lisp
  (use-package corfu
    :ensure t
    :init
    (global-corfu-mode) ;; 전역 모드 활성화
    :custom
    (corfu-auto t)                 ;; 타이핑 시 자동으로 팝업 띄우기 (Company의 idle-delay 역할)
    (corfu-auto-delay 0.2)         ;; 0.2초 후 팝업 (취향에 맞게 조절)
    (corfu-auto-prefix 2)          ;; 2글자 이상 쳤을 때 팝업
    (corfu-cycle t)                ;; 목록 끝에서 처음으로 순환
    (corfu-quit-no-match 'separator) ;; 매칭되는 게 없으면 팝업 끄기
    (corfu-preselect 'prompt)      ;; 첫 번째 항목을 바로 선택하지 않음 (엔터 실수 방지)
    :bind
    (:map corfu-map
          ("SPC" . corfu-insert-separator) ;; 공백 입력 시 필터링 구분자로 사용
          ("TAB" . corfu-next)
          ([tab] . corfu-next)
          ("S-TAB" . corfu-previous)
          ([backtab] . corfu-previous)))

  (use-package nerd-icons-corfu
    :ensure t
    :after corfu
    :config
    (add-to-list 'corfu-margin-formatters #'nerd-icons-corfu-formatter))

  (use-package cape
    :ensure t
    :init
    ;; 기본 completion-at-point-functions에 cape 기능들을 추가
    (add-to-list 'completion-at-point-functions #'cape-dabbrev) ;; 현재 버퍼 단어
    (add-to-list 'completion-at-point-functions #'cape-file)    ;; 파일 경로
    (add-to-list 'completion-at-point-functions #'cape-elisp-block)) ;; Org 모드 내 코드 블록
#+END_SRC

** IVY

#+BEGIN_SRC emacs-lisp
  (use-package counsel
    :ensure t
    :after ivy
    :config (counsel-mode))

  (use-package ivy
    :ensure t
    :diminish
    :bind
    ;; ivy-resume resumes the last Ivy-based completion.
    (("C-c C-r" . ivy-resume)
     ("C-x B" . ivy-switch-buffer-other-window))
    :config
    (ivy-mode))

  (use-package all-the-icons-ivy-rich
    :ensure t
    :init (all-the-icons-ivy-rich-mode 1))

  (use-package ivy-rich
    :after ivy
    :ensure t
    :init (ivy-rich-mode 1)
    :custom 
    (ivy-virtual-abbreviate 'full
  			  ivy-rich-switch-buffer-align-virtual-buffer t
  			  ivy-rich-path-style 'abbrev))
#+END_SRC

** Git (Magit)

#+BEGIN_SRC emacs-lisp
  ;;(use-package magit-section 
  ;;  :ensure t)
  (use-package cond-let
    :ensure t)

  (use-package transient
    :ensure t)

  (use-package magit
    :ensure t
    :after (cond-let transient)
    :commands (magit-status)
    :bind (("C-x g" . magit-status))
    :config
    (setf (alist-get 'unpushed magit-section-initial-visibility-alist) 'show))



#+END_SRC

#+RESULTS:
: [nil 26768 3830 347673 nil elpaca-process-queues nil nil 0 nil]

** Markdown
#+BEGIN_SRC emacs-lisp
  (use-package markdown-mode
    :ensure t
    :mode (("README\\.md\\'" . gfm-mode)
           ("\\.md\\'" . markdown-mode)
           ("\\.markdown\\'" . markdown-mode))
    :init (setq markdown-command "multimarkdown"))

  (use-package mermaid-mode :ensure t
    :after markdown-mode
    )

  ;; markdown -> org 기능
  (defun markdown-convert-buffer-to-org ()
    "Convert the current buffer's content from markdown to orgmode format and save it with the current buffer's file name but with .org extension."
    (interactive)
    (shell-command-on-region (point-min) (point-max)
                             (format "pandoc -f markdown -t org -o %s"
                                     (concat (file-name-sans-extension (buffer-file-name)) ".org"))))
#+END_SRC


** Hide Show mode
#+BEGIN_SRC emacs-lisp
  (add-hook 'prog-mode-hook #'hs-minor-mode)
#+END_SRC




** JsonRPC
#+BEGIN_SRC emacs-lisp
  (use-package jsonrpc
    :ensure t)
#+END_SRC

** Eglot
#+BEGIN_SRC emacs-lisp

  (use-package eglot
    ;;:ensure (:inherit elpaca-menu-gnu-devel-elpa)
    :ensure nil
    :demand t
    :after (jsonrpc)
    :hook
    (((c-mode c-ts-mode c++-mode c++-ts-mode c-or-c++-mode c-or-c++-ts-mode) . eglot-ensure)
     (d-mode . eglot-ensure)
     (typescript-ts-mode . eglot-ensure)
     (tsx-ts-mode . eglot-ensure)
     (js-ts-mode . eglot-ensure))
    :config 
    (add-to-list 'eglot-server-programs '((c-mode c-ts-mode c++-mode c++-ts-mode c-or-c++-mode c-or-c++-ts-mode) . ("ccls" "--init"
                                                                                                                    "{\"clang\": {
            \"extraArgs\": [
              \"-isystem/usr/local/include\", \"-isystem/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/lib/clang/12.0.0/include\", 
              \"-isystem/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/include\",
              \"-isystem/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/usr/include\", 
              \"-isystem/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks\",
              \"-isystem/opt/homebrew/include\"
            ],
            \"resourceDir\": \"/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/lib/clang/12.0.0\"
          }
        }")))
    (add-to-list 'eglot-server-programs '(d-mode . ("serve-d")))
    (add-to-list 'eglot-server-programs '((odin-mode odin-ts-mode) . ("ols"))))
  (electric-pair-mode 1)
  (elpaca-wait)
#+END_SRC

** paredit

#+begin_src emacs-lisp
  (use-package paredit
    :ensure t
    :hook 
    (clojure-mode . enable-paredit-mode)
    (cider-repl-mode . enable-paredit-mode)
    (emacs-lisp-mode . enable-paredit-mode))
#+end_src

* 프로그래밍 언어

** Common Lisp

#+BEGIN_SRC emacs-lisp
  ;; Common Lisp 로딩
  (use-package slime 
    :ensure t
    :init
    (load (expand-file-name "~/quicklisp/slime-helper.el") t))

  ;; (use-package slime-autoloads :ensure t)


  (setq inferior-lisp-program
        (cond ((eq system-type 'gnu/linux) "/usr/bin/sbcl")
              ((eq system-type 'darwin) "/usr/local/bin/sbcl")))


  (add-hook 'lisp-mode-hook (lambda () (slime-mode t)))
  (add-hook 'inferior-lisp-mode-hook (lambda () (inferior-slime-mode t)))

  ;; hyper spec
  (load (expand-file-name "~/quicklisp/clhs-use-local.el") 'noerror)
  ;; (setq common-lisp-hyperspec-root
  ;;      (concat "file://" (expand-file-name "~/doc/common-lisp/HyperSpec/")))

  (setq browse-url-handlers '(("http://lispworks.com" . eww-browse-url)
                              ("file://" . eww-browse-url)
                              ("" . browse-url-default-browser)))

  (use-package ac-slime
    :ensure t
    :config
    (add-hook 'slime-mode-hook 'set-up-slime-ac)
    (add-hook 'slime-repl-mode-hook 'set-up-slime-ac)
    (eval-after-load "auto-complete"
      '(add-to-list 'ac-modes 'slime-repl-mode)))

  (provide 'prelude-lisp)

#+END_SRC




** Clojure
#+begin_src emacs-lisp
  (use-package clojure-mode 
    :ensure t)
#+end_src

** C++

#+BEGIN_SRC emacs-lisp

  (use-package ccls
    :ensure t 
    :config
    (setq ccls-executable "~/.local/bin/ccls"))

#+END_SRC


** D-lang
#+BEGIN_SRC emacs-lisp
  (use-package d-mode
    :ensure t
    :hook (d-mode . corfu-mode))
#+END_SRC



** Rust  (RUST mode)
#+BEGIN_SRC emacs-lisp

  (use-package rust-mode
    :ensure t
    :after eglot
    :init
    ;; tree-sitter 지원 활성화
    (setq rust-mode-treesitter-derive t)
    ;; Emacs 29 이상일 경우 기본적으로 rust-ts-mode를 사용하도록 리매핑
    (when (boundp 'major-mode-remap-alist)
      (add-to-list 'major-mode-remap-alist '(rust-mode . rust-ts-mode)))
    
    :custom
    (rust-format-on-save t)
    ;; PATH 환경 변수가 에디터에 잘 잡혀있다면 이 절대경로들은 생략할 수도 있습니다.
    (rust-rustfmt-bin (expand-file-name "~/.cargo/bin/rustfmt"))
    (rust-cargo-bin (expand-file-name "~/.cargo/bin/cargo"))

    ;; AI 도구 및 기타 유용한 단축키 바인딩
    :bind (:map rust-mode-map
                ("C-c C-c" . rust-run)
                ("C-c g"   . gptel-menu)  ;; AI에게 현재 맥락을 기반으로 질문/리팩토링 요청
                ("C-c s"   . gptel-send)) ;; 선택한 영역의 코드를 바로 AI로 전송하여 인라인 응답 받기
    

    :hook
    ((clojure-mode clojurescript-mode) . eglot-ensure)
    ((rust-mode rust-ts-mode) . eglot-ensure)
    ((rust-mode rust-ts-mode) . corfu-mode)
    ((rust-mode rust-ts-mode) . prettify-symbols-mode)
    ((rust-mode rust-ts-mode) . (lambda ()
  				;; 전역 오염을 막기 위해 반드시 setq-local 사용
  				(setq-local indent-tabs-mode nil
                                              tab-width 4
                                              c-basic-offset 4
                                              fill-column 120)))

    :config
    ;; eglot이 로드된 이후에 서버 프로그램을 등록합니다.
    (with-eval-after-load 'eglot
      (add-to-list 'eglot-server-programs
  		 `((rust-mode rust-ts-mode) . (,(expand-file-name "~/.local/bin/rust-analyzer")))))
    )

#+END_SRC

** PASCAL (FPC)
#+BEGIN_SRC emacs-lisp
  (use-package opascal-mode
    :ensure nil
    :after eglot
    :init
    (add-to-list 'auto-mode-alist
                 '("\\.\\(pas\\|pp\\|lpr\\|dpr\\)\\'" . opascal-mode))
    (add-to-list 'eglot-server-programs `((pascal-mode delphi-mode opascal-mode) . (,(expand-file-name "~/.local/bin/pasls") 
                                                                                    :initializationOptions (:PP "~/.local/fpcupdeluxe/fpc/bin/aarch64-darwin/fpc.sh"
                                                                                                                :FPCDIR ,(expand-file-name "~/.local/fpcupdeluxe/fpsrc")
                                                                                                                :LAZARUSDIR ,(expand-file-name "~/.local/fpcupdeluxe/lazarus")
                                                                                                                :FPCTARGET "aarch64-darwin"
                                                                                                                :FPCTARGETCPU "aarch64"))))
    ;; (tree-sitter-require 'opascal)
    :hook
    ((opascal-mode . corfu-mode)
     (opascal-mode . tree-sitter-mode)
     (opascal-mode . eglot-ensure)))
#+END_SRC

** Typescript

#+begin_src emacs-lisp
  (setq major-mode-remap-alist
        (append 
         '((typescript-mode . typescript-ts-mode)
  	 (js-mode . js-ts-mode)
  	 (css-mode . css-ts-mode)
  	 (json-mode . json-ts-mode))
         major-mode-remap-alist))
#+end_src



** Cider

#+begin_src emacs-lisp

  (use-package cider
    :ensure t
    :custom
    (cider-repl-display-help-banner nil)  ; REPL 시작 시 불필요한 배너 숨김
    (cider-repl-pop-to-buffer-on-connect t) ; 연결 시 REPL 창으로 포커스
    (cider-save-file-on-load t)           ; 코드를 REPL에 로드할 때 파일 자동 저장
    ;; ClojureScript REPL 기본값 설정 (주로 shadow-cljs나 figwheel을 많이 사용함)
    (cider-default-cljs-repl 'shadow) 
    :hook
    (cider-mode . eldoc-mode) ; 함수 시그니처를 미니버퍼에 표시
    (cider-repl-mode . corfu-mode))
#+end_src

* 기타


** Backup 및 임시파일 경로 수정

#+BEGIN_SRC emacs-lisp

  (defun yhj/backup-file-name (fpath)
    "Return a new file path of a given file path.
  If the new path's directories does not exist, create them."
    (let* ((backupRootDir "~/.emacs.d/emacs-backup/")
  	 (filePath (replace-regexp-in-string "[A-Za-z]:" "" fpath )) ; remove Windows driver letter in path
  	 (backupFilePath (replace-regexp-in-string "//" "/" (concat backupRootDir filePath "~") )))
      (make-directory (file-name-directory backupFilePath) (file-name-directory backupFilePath))
      backupFilePath))
  (setq make-backup-file-name-function 'yhj/backup-file-name)

#+END_SRC

** Sudo Edit

#+BEGIN_SRC emacs-lisp
  (use-package sudo-edit
    :ensure t)
#+END_SRC

** Exec Path from shell
#+BEGIN_SRC emacs-lisp
  (use-package exec-path-from-shell
    :ensure t
    :config 
    (exec-path-from-shell-initialize))
#+END_SRC

** Android Tramp 설정
#+BEGIN_SRC emacs-lisp
  (use-package tramp
    :ensure t
    :config
    (connection-local-set-profile-variables
     'tramp-connection-local-termux-profile
     `((tramp-remote-path
        . ,(mapcar
            (lambda (x)
              (if (stringp x) (concat "/data/data/com.termux/files" x) x))
            (copy-tree tramp-remote-path)))))

    (connection-local-set-profiles
     '(:application tramp :machine "192.168.196.169")
     'tramp-connection-local-termux-profile)


    (add-to-list 'tramp-connection-properties
                 (list (regexp-quote "android") "remote-shell" "sh"))
    (add-to-list 'tramp-connection-properties
                 (list (regexp-quote "android")
                       "tmpdir" "/data/data/com.termux/files/home/tmp"))
    (connection-local-set-profiles
     '(:application tramp :machine "android")
     'tramp-connection-local-termux-profile))
#+END_SRC

** ODIN
*** Odin Mode


#+begin_src emacs-lisp
  ;; Enable odin-mode and configure OLS as the language server
  (use-package odin-mode
    :after eglot
    :ensure (:host github :repo "mattt-b/odin-mode")
    :mode ("\\.odin\\'" . odin-mode)
    :hook (odin-mode . eglot-ensure)
    :config
    (with-eval-after-load 'eglot 
      (add-to-list 'eglot-server-programs '((odin-mode  odin-ts-mode) . ("ols")))))

#+end_src

** Ollama

#+BEGIN_SRC emacs-lisp
  (use-package ellama
    :ensure t
    :init
    (require 'llm-ollama)
    (setopt ellama-language "English")
    (setopt ellama-provider
  	  (make-llm-ollama
  	   :chat-model "codellama:7b" 
  	   :embedding-model "nomic-embed-text"
  	   :default-chat-non-standard-params '(("num_ctx" . 8192))))
    (setopt ellama-coding-provider
  	  (make-llm-ollama
  	   :chat-model "qwen2.5-coder:7b"
  	   :embedding-model "nomic-embed-text"
  	   :default-chat-non-standard-params '(("num_ctx" . 32768)))))

#+END_SRC

** TreeSitter 

Lua 의 경우 버전 낮춘다.
#+begin_src emacs-lisp
  (defvar treesit-language-source-alist nil)
  (let ((new-langs
         '((lua "https://github.com/tree-sitter-grammars/tree-sitter-lua" "v0.3.0")
  	 (javascript "https://github.com/tree-sitter/tree-sitter-javascript" "master" "src")
  	 (tsx "https://github.com/tree-sitter/tree-sitter-typescript" "master" "tsx/src")
  	 (typescript "https://github.com/tree-sitter/tree-sitter-typescript" "master" "typescript/src"))))
    
    ;; 2. 반복문을 돌면서 기존 설정을 갱신
    (dolist (lang-def new-langs)
      (let ((lang-symbol (car lang-def)))
        ;; (1) 기존 리스트에서 해당 언어(키)가 있다면 삭제
        (setq treesit-language-source-alist
              (assq-delete-all lang-symbol treesit-language-source-alist))
        ;; (2) 새 설정을 리스트에 추가
        (add-to-list 'treesit-language-source-alist lang-def))))

#+end_src

** Format All
#+begin_src emacs-lisp
  (use-package format-all
    :ensure t
    :preface
    (defun ian/format-code ()
      "Auto-format whole buffer."
      (interactive)
      (if (derived-mode-p 'prolog-mode)
          (prolog-indent-buffer)
        (format-all-buffer)))
    :config
    (global-set-key (kbd "M-F") #'ian/format-code)
    (add-hook 'prog-mode-hook #'format-all-ensure-formatter)
    )

#+end_src

* 바이브 코딩  

** Aider

#+begin_src emacs-lisp

  (use-package aidermacs
    :ensure t
    :bind (("C-c a" . aidermacs-transient-menu))
    :custom
    ;;See the Configuration section below
    (aidermacs-default-chat-mode 'architect)
    ;;(aidermacs-default-model "gemini")
    )

#+end_src

** gptel

#+begin_src emacs-lisp
  (use-package gptel
    :ensure t
    :bind (("C-c RET" . gptel-send))
    :config
    (require 'org)
    (require 'transient)
    (require 'gptel-integrations)
    (require 'gptel-org)
    ;; 1. API 키를 임시로 저장할 변수 선언 (초기값 nil)
    (defvar my/gemini-api-key-cache nil
      "API 키를 현재 Emacs 세션 동안만 저장하는 변수")

    (defvar my/openrouter-api-key-cache nil
      "API 키를 현재 Emacs 세션 동안만 저장하는 변수 : openrouter 용")

    ;; 2. 키를 요청하는 함수 정의
    (defun my/get-gemini-key ()
      "변수에 키가 있으면 반환하고, 없으면 사용자에게 비밀번호처럼 물어본 뒤 저장합니다."
      (or my/gemini-api-key-cache
          ;; read-passwd는 입력 문자를 *로 가려줍니다.
          (setq my/gemini-api-key-cache (read-passwd "Gemini API Key를 입력하세요: "))))

    (defun my/get-openrouter-key ()
      "변수에 키가 있으면 반환하고, 없으면 사용자에게 비밀번호처럼 물어본 뒤 저장합니다."
      (or my/openrouter-api-key-cache
          ;; read-passwd는 입력 문자를 *로 가려줍니다.
          (setq my/openrouter-api-key-cache (read-passwd "Open Router API Key를 입력하세요: "))))

    ;; 3. Gemini 설정에 '함수'를 전달
      ;;; (gptel-make-gemini "Gemini"
      ;;;   :key #'my/get-gemini-key  ;; 여기가 핵심입니다 (문자열 변수 대신 함수명 전달)
      ;;;   :stream t)

    ;; 4. 기본 모델 설정
      ;;; (setq gptel-model 'gemini-2.5-flash) ;; 기본 gemini 사용함 
      ;;; (setq gptel-backend (gptel-make-gemini "Gemini" :key #'my/get-gemini-key :stream t))

    ;; Gemini 백엔드 
    (defvar gptel--gemini-backend 
      (gptel-make-gemini "Gemini"
        :key #'my/get-gemini-key  ;; 여기가 핵심입니다 (문자열 변수 대신 함수명 전달)
        :stream t))


    ;; OpenRouter 백엔드
    (defvar gptel--openrouter-backend
      (gptel-make-openai "OpenRouter"
        :host "openrouter.ai"
        :endpoint "/api/v1/chat/completions"
        :stream t
        :key #'my/get-openrouter-key
        :models '(google/gemma-3-27b-it:free
                  arcee-ai/trinity-large-preview:free
                  deepseek/deepseek-r1-0528:free
                  meta-llama/llama-3.3-70b-instruct:free
                  openai/gpt-oss-120b:free)))

    ;; 로컬 Llama.cpp 백엔드
    (defvar gptel--local-backend
      (gptel-make-openai "Local-Llama"
        :host "localhost:48080"
        :protocol "http"
        :stream t
        :endpoint "/v1/chat/completions"
        :key "openaillama"
        :models '(qwen2.5-coder)))
    
    (setq gptel-model 'qwen2.5-coder
    	gptel-backend gptel--local-backend)


    ;; 5. org 파일을 파싱하여 지시문(Directives)로 변환하는 함수 
    (defun my/gptel-parse-org-directives (file)
      "ORG 파일을 읽어 (KEY . CONTENT) 형태 리스트로 만들기"
      (with-temp-buffer 
        (insert-file-contents file)
        (org-mode)
        (let (directives)
    	(org-map-entries 
    	 (lambda ()
    	   (let* ((heading (org-get-heading t t t t))
    		  (key (intern (replace-regexp-in-string " " "-" (downcase heading))))
    		  (content (org-get-entry)))
    	     (when (and heading content)
    	       (push (cons key content) directives)))))
    	(nreverse directives))))

    ;; 6. 메뉴에서 실행할 명령어 정의 
    (defun my/gptel-load-directives-from-menu (file)
      "메뉴에서 파일을 선택해 gptel-directives에 추가"
      (interactive "fSelect Directives Org File: ") 
      (let ((new-directives (my/gptel-parse-org-directives file)))
        (if new-directives 
    	  (progn 
    	    (setq gptel-directives (append gptel-directives new-directives))
    	    (message "Loaded %d directives from %s" (length new-directives) file))
    	(message "No directives found in file."))))

    ;; 7. gptel-menu 
    (with-eval-after-load 'gptel-transient 
      (transient-append-suffix 'gptel-menu 'gptel--infix-add-directive
        '("L" "Load Directives File" my/gptel-load-directives-from-menu))
      )
    (add-to-list 'gptel-prompt-prefix-alist `(org-mode . ,(concat "*** ian " (format-time-string "[%Y-%m-%d]") "\n")))
    :custom
    (gptel-default-mode 'org-mode)
    (gptel-expert-commands t)
    (gptel-track-media t)
    (gptel-include-reasoning 'ignore)
    (gptel-prompt-prefix-alist
     '((markdown-mode . "### ") (org-mode . "*** ian\12") (text-mode . "### ")))
    (gptel-log-level 'info)
    )
#+end_src


** GPTEL MCP

#+begin_src emacs-lisp 

  (use-package mcp 
    :ensure t
    :after gptel
    :custom (mcp-hub-servers
             `(("filesystem" . (:command "npx" 
  				       :args (
  					      "-y" 
  					      "@modelcontextprotocol/server-filesystem"
  					      "/Users/siabard/sandbox"
  					      )
  				       :roots ("/Users/siabard/sandbox/")))
               ("fetch" . (:command "uvx" :args ("mcp-server-fetch")))
  	     ("mcp-shell-server" . (:command "uvx"
  					     :args ("mcp-shell-server")
  					     :env (:ALLOW_COMMANDS "bc,cat,chmod,curl,date,echo,emacs,find,git,grep,gunzip,head,jq,kubectl,ls,lynx,npm,psql,pwd,python,rg,sed,sqlite3,tail,unzip,wc,sbcl")))))
    :config (require 'mcp-hub)
    :hook (after-init . mcp-hub-start-all-server))
#+end_src

** GPTEL directives 

#+begin_src emacs-lisp

  (use-package gptel-prompts
    ;; git clone git@github.com:jwiegley/gptel-prompts.git 
    :ensure (:host github :repo "jwiegley/gptel-prompts")
    :after gptel
    :config
    (setq gptel-prompts-directory (expand-file-name "~/project/ai/composable-directives"))
    (gptel-prompts-update)
    ;; ensure prompts are updated if prompt files change
    (gptel-prompts-add-update-watchers))          

#+end_src

#+begin_src emacs-lisp
  (defun my/gptel-compose-prompts ()
    "composable-prompts 디렉토리에서 여러 파일을 선택해 하나의 시스템 프롬프트로 병합합니다.
  여러 개를 선택할 때는 콤마(,)로 구분하세요."
    (interactive)
    (let* ((prompt-dir (expand-file-name "~/project/ai/composable-directives"))
           ;; 디렉토리 내의 모든 .md 및 .txt 파일 검색
           (files (directory-files-recursively prompt-dir "\\.md\\'\\|\\.txt\\'"))
           ;; 파일 경로를 디렉토리 기준 상대 경로로 변환 (보기 좋게)
           (file-names (mapcar (lambda (f) (file-relative-name f prompt-dir)) files))
           ;; completing-read-multiple (CRM)을 사용하여 여러 파일 선택
           (selected (completing-read-multiple "프롬프트 블록 선택 (콤마로 구분): " file-names))
           (combined-prompt ""))
      
      (when selected
        ;; 선택된 파일들의 내용을 하나로 이어 붙임
        (dolist (sel selected)
          (let ((filepath (expand-file-name sel prompt-dir)))
            (with-temp-buffer
              (insert-file-contents filepath)
              (setq combined-prompt (concat combined-prompt 
                                            (format "\n\n### [%s]\n" (file-name-base sel))
                                            (buffer-string))))))
        
        ;; 임시 Directives 이름 생성 및 gptel에 적용
        (let ((directive-name (intern (mapconcat 'file-name-base selected "+"))))
          (add-to-list 'gptel-directives (cons directive-name combined-prompt))
          (setq gptel--system-message combined-prompt)
          (message "✅ 프롬프트 조합 완료: %s" directive-name)))))

  ;; 단축키 등록
  (with-eval-after-load 'gptel
    (define-key gptel-mode-map (kbd "C-c g c") 'my/gptel-compose-prompts))

#+end_src

** GPTEL 로 Common lisp Refactoring 직접 요청 

#+begin_src emacs-lisp

  (defun my/gptel-refactor-lisp-defun ()
    "현재 커서가 위치한 Lisp 함수(defun)를 잡아서 gptel에게 리팩토링을 요청합니다."
    (interactive)
    (let* ((defun-str (save-excursion
                        (mark-defun) ;; 현재 함수 전체 선택
                        (buffer-substring-no-properties (region-beginning) (region-end))))
           (prompt (read-string "Refactor Request (e.g., Optimize, Add comments): "
                                "Optimize this Common Lisp code and explain why.")))
      ;; gptel 버퍼를 열고 내용을 전송
      (gptel-request
       (concat prompt "\n\n```lisp\n" defun-str "\n```")
        :system "You are a Common Lisp Expert."
        :context (list (cons 'major-mode major-mode)) ;; 현재 모드 정보 전달
        :callback (lambda (response info)
                    (if response
                        (with-current-buffer (get-buffer-create "*Gptel-Refactor*")
  			(erase-buffer)
  			(insert response)
  			(common-lisp-mode) ;; 결과창도 리스프 모드로
  			(display-buffer (current-buffer)))
                      (message "Gptel request failed."))))))

  ;; 키바인딩 (Lisp 모드에서만 동작)
  (with-eval-after-load 'slime
    (define-key slime-mode-map (kbd "C-c g r") 'my/gptel-refactor-lisp-defun))

#+end_src


** GPTEL tool

#+begin_src emacs-lisp
  (defun my-agent-read-file (filepath)
    "저장된 FILEPATH의 파일 내용을 읽어서 문자열로 반환한다. 
    파일이 없거나 읽을 수 없는 경우 에러 메세지를 반환하여 에이전트가 상황을 인지하게 한다."
    (let ((expanded-path (expand-file-name filepath)))
      (if (file-readable-p expanded-path)
    	(with-temp-buffer 
    	  ;; 파일을 임시 버퍼에 삽입하고 그 내용을 문자열로 추출 
    	  (insert-file-contents expanded-path)
    	  (buffer-string))
        ;; 에이전투가 실패 원인을 알도록 에러 반환 
        (format "Error: Cannot read file or file is not exist. path: %s" expanded-path))))

#+end_src

#+begin_src emacs-lisp
  (defun my-agent-write-file (filepath content)
    "FILEPATH에 CONTENT를 덮어씌운다. 상위 디렉토리가 없다면 자동으로 생성한다. 성공 또는 실패 메세지를 반환하여 에이전트가 결과를 인지하게 한다."

    (condition-case err 
        (let* ((expanded-path (expand-file-name filepath))
  	     (dir-path (file-name-directory expanded-path)))
  	;; 상위 경로가 존재하지않으면 생성 (t 옵션으로 부모 디렉토리까지 생성)
  	(unless (file-exists-p dir-path)
  	  (make-directory dir-path t))
  	;; 임시 버퍼를 열어 내용을 삽입하고 파일로 저장 (기존 내영은 덮어씌움)
  	(with-temp-file expanded-path
  	  (insert content))

  	;; 에이전트에게 전달할 성공 메세지 
  	(format "Success: 파일 작성 완료. 경로: %s" expanded-path))
      ;; 권한 문제 등으로 에러 발생시 에이전트에게 에러 내용 전달
      (error (format "Error: 파일 작성 실패. 원인: %s" (error-message-string err)))))

#+end_src

#+begin_src emacs-lisp
  (defun my-agent-patch-file (filepath old-text new-text)
    "FILEPATH의 파일에서 정확히 일치하는 OLD-TEXT를 찾아 NEW-TEXT로 교체한다.
  일치하는 구간이 없거나 여러 개일 경우 에이전트에게 에러를 변환하여 수정을 유도한다"
    (let ((expanded-path (expand-file-name filepath)))
      (condition-case err
  	(if (not (file-writable-p expanded-path))
  	    (format "Error: 파일이 존재하지않거나 쓰기 권한이 없습니다. 경로: %s" expanded-path)
  	  (with-temp-buffer
  	    ;; 파일 내용을 임시 버퍼로 불러옴 
  	    (insert-file-contents expanded-path)
  	    (goto-char (point-min))

  	    ;; old-text가 파일 내에 몇 번 등장하는지 카운트 
  	    (let ((count 0))
  	      (while (search-forward old-text nil t)
  		(setq count (1+ count)))
  	      
  	      (cond 
  	       ((= 0 count)
  		;; 에이전트가 흔히 하는 실수(들여쓰기, 줄바꿈 불일치)를 가이드 
  		"Error: old_text를 파일에서 찾을 수 없습니다. 공백, 줄바꿈, 들여쓰기가 원본 파일과 *정확히* 일치하는지 확인하세요.")
  	       
  	       ((> count 1)
  		;; 중복 매칭 시 엉뚱한 곳을 수정하는 것을 방지
  		(format "Error: old_text가 파일 내에 %d 번 존재하여 모호합니다. 주변 코드를 더 포함하여 old_text가 파일 내에서 유일해지도록 다시 시도하세요." count))
  	       
  	       (t
  		;; 정확히 1번 매칭되었을 때만 교체 수행
  		(goto-char (point-min))
  		(search-forward old-text)
  		(replace-match new-text t t)

  		;; 변경된 내용을 실제 파일에 저장
  		(write-region (point-min) (point-max) expanded-path)
  		(format "Success: %s 파일의 코드가 성공적으로 수정되었습니다" expanded-path))))))

        ;; 예기치 않은 Emacs Lisp 에러 처리 
        (error (format "Error: 패치 실행 중 시스템 오류 발생: %s" (error-message-string err))))))
#+end_src


#+begin_src emacs-lisp 
  ;; tool 이 comand shell 을 실행할 수 있는 복수 디렉토리 변수 
  (defvar my-agent-allowed-shell-directories 
    '("/Users/siabard/sandbox")
    "에이전트가 명령어를 실행할 수 있는 허용된 샌드박스 디렉토리 목록. 필요에 따라 경로를 추가하시오. "
    )

  (defvar my-agent-allowed-shell-commands 
    '("bc" "cat" "chmod" "curl" "date" "echo" "emacs" "find" "git" "grep" "gunzip" "head" "jq" "kubectl" "ls" "lynx" "npm" "psql" "pwd" "python" "rg" "sed" "sqlite3" "tail" "unzip" "wc" "sbcl") 
    "에이전트가 실행하도록 허용된 명령어 목록"
    )

  (defun my-agent-run-shell-safe (dir command-string)
    "지정된 DIR이 유효한지 확인하고, COMMAND-STRING의 실행 파일이 존재하는지 검증한 후 실행한다. 쉘을 거치지 않고(call-process) 직접 실행하여 파이프(|) 나 리다이렉션(>) 을 통한 우회를 방지한다."

    (let* ((expanded-dir (expand-file-name dir))
           ;; "pytest -v test_main.py" 같은 문자열을 '("pytest" "-v" "test_main.py")로 분리 
  	 (expanded-allowed-dirs (mapcar #'expand-file-name my-agent-allowed-shell-directories))
           (args (split-string-and-unquote command-string))
           (program (car args)) ;; 실행 파일 (예: pytest)
           (program-args (cdr args)) ;; 인자 목록 (예: '("-v" "test_main.py"))
           (exit-code 0)
           (output-string "")
           (max-output-length 4000)
  	 (is-in-allowed-dir nil))


      (dolist (base-dir expanded-allowed-dirs)
        (when (file-in-directory-p expanded-dir base-dir)
  	(setq is-in-allowed-dir t)))

      (condition-case err
          (cond 
  	 ;; 제약1. 복수 Filesystem 샌드박스 검증 
  	 ((not is-in-allowed-dir)
  	  (format "Error: 보안 정책 위반. 허용된 샌드박스 디렉토리 외부에서는 명령어를 실행할 수 없습니다.\n요청된 경로: %s\n허용된 샌드박스: %s" 
  		  expanded-dir
  		  (string-join expanded-allowed-dirs ", ")))

  	 ;; 제약2. Shell 명령어 allowed 리스트 
  	 ((not (member program my-agent-allowed-shell-commands))
  	  (format "Error: 보안 정책 위반. 허용되지 않은 명령어 '%s' 입니다. 허용된 명령어: %s"
  		  program
  		  (string-join my-agent-allowed-shell-commands ", ")))

           ;; 검증 1: 디렉토리 실제 존재 확인 
           ((not (file-directory-p expanded-dir))
    	  (format "Error: 지정된 디렉토리가 존재하지 않습니다: %s" expanded-dir))

  	 ;; 검증 2: 첫번째 단어(program)가 시스템 경로에 있는 실행 가능한 파일인지 확인
  	 ((not (executable-find program))
  	  (format "Error: '%s' 명령어를 찾을 수 없거나 실행 권한이 없습니다. 쉘 내장 명령어(cd, echo 등)나 파이프(|)는 사용할 수 없습니다." program))

  	 ;; 검증 통과
  	 (t
  	  (let ((default-directory expanded-dir))
  	    (with-temp-buffer
  	      ;; call-process는 쉘을 호출하지않고 OS레벨에서 직접 바이너리를 호출 
  	      (setq exit-code (apply #'call-process program nil t nil program-args))
  	      (setq output-string (buffer-string))

  	      ;; 출력 길이 제한 
  	      (when (> (length output-string) max-output-length)
  		(setq output-string
  		      (concat (substring output-string 0 max-output-length)
  			      "\n\n... [Output truncated: 출력이 너무 깁니다. 더 구체적인 인자를 주어 실행하세요.] ...")))
  	      (format "Exit Code: %d\nOutput:\n%s " exit-code output-string)))))
        (error (format "Error: 시스템 오류 발생: %s" (error-message-string err))))))

#+end_src




#+begin_src emacs-lisp
    (with-eval-after-load 'gptel
      (gptel-make-tool
       :name "read_file"
       :category "harness"
       :description "Reads the contents fo a specified file and returns it as a string. Use this to inspect existing code, logs, or configuration files before making modifications."
       :args '((:name "filepath" :type string :description "The absolute or relative path to the file you want to read"))
       :function #'my-agent-read-file)

      (gptel-make-tool 
       :name "write_file"
       :category "harness"
       :function #'my-agent-write-file
       :description "Writes or overwrites the given content to the specified file. If the file already exists, its contents will be completely replaced. Directories are created automatically if needed. Use this to create new files or fully update existing ones."
       :args '((:name "filepath" :type string :description "The absolute or relative path to the file you want to write")
    	   (:name "content" :type string :description "The complete text content to write into file file.")))
      
      ;; Agent를 위해서는 '_'를 써야하지만 emacs-lisp 에서는 '-'를 많이 쓰는 부분을
      ;; 고려하여 :name 에 들어가는 이름과 실제 function의 파라미터를 잘 구분할 것
      ;; 아울러 출력 메세지는 LLM이 인지해야하므로 Agent를 위한 '_'를 쓸 것
      (gptel-make-tool 
       :name "patch_file"
       :category "harness"
       :function #'my-agent-patch-file
       :description "Replaces a specific text block (old_text) with new text (new_text) in a file. Use this to modify existing files without rewriting the entire file. The old_text must match the target file exactly, including all whitespace and indentation. If the match is ambiguous, include more surrounding lines in old_text."
       :args '((:name "filepath" :type string :description "The absolute or relative path to the file to modify.")
    	   (:name "old_text" :type string :description "The exact text block to be replaced. Must be entirely unique within the file.")
    	   (:name "new_text" :type string :description "The new text block that will replace old_text."))
       )

      (gptel-make-tool
       :name "run_shell_command"
       :function #'my-agent-run-shell-safe
       :category "harness"
       :description (format "Safely executes a program. 
  [STRICT SECURITY CONSTRAINTS]
  1. Directory Sandbox: You CANNOT execute commands outside of these allowed directories (or their subdirectories): %s
  2. Command Allowlist: You are ONLY allowed to run the following commands: %s.
  Do NOT use shell pipes (|), redirects (>), or operators (&&)." 
                          ;; [개선] Gemini에게 전달하는 설명서에도 미리 절대 경로로 풀어서(Expand) 제공
                            (string-join (mapcar #'expand-file-name my-agent-allowed-shell-directories) ", ")
                          (string-join my-agent-allowed-shell-commands ", "))
       :args '((:name "dir" :type string :description "REQUIRED: The directory to run the command. Must be strictly within one of the allowed sandboxes.")
  	       (:name "command_string" :type string :description "The allowed executable and its arguments (e.g., 'npm test').")))

      (gptel-make-tool
       :name "read_pdf"
       :description "주어진 경로의 PDF 파일에서 텍스트를 추출하여 읽어옵니다. PDF 문서의 내용을 파악하거나 요약해야 할 때 사용하세요."
       :args (list '(:name "filepath" :type string :description "읽어올 PDF 파일의 절대 경로 (예: /Users/siabard/sandbox/document.pdf)"))
       :category "file"
       :function (lambda (filepath)
                   (cond
        		;; 1. 파일 존재 여부 확인
        		((not (file-exists-p filepath))
        		 (format "Error: 파일을 찾을 수 없습니다: %s" filepath))
        		
        		;; 2. CLI 도구 설치 여부 확인
        		((not (executable-find "pdftotext"))
        		 "Error: 시스템에 'pdftotext' 명령어가 없습니다. 터미널에서 'brew install poppler'를 실행해 주세요.")
        		
        		;; 3. 텍스트 추출 실행
        		(t
        		 ;; pdftotext <파일경로> - (표준 출력으로 텍스트 반환)
        		 (let ((text (shell-command-to-string 
                                  (format "pdftotext %s -" (shell-quote-argument filepath)))))
                       (if (string-empty-p (string-trim text))
                           "Error: 문서에서 텍스트를 추출할 수 없습니다. (스캔된 이미지로만 구성된 PDF일 수 있습니다.)"
                         text))))))


      (gptel-make-tool
       :name "read_pypdf"
       :description "주어진 경로의 PDF 파일에서 텍스트를 추출하여 읽어옵니다. PDF 문서의 내용을 파악하거나 요약해야 할 때 사용하세요."
       :args '((:name "filepath" :type string :description "읽어올 PDF 파일의 절대 경로 (예: /Users/siabard/sandbox/document.pdf)"))
       :category "file"
       :function (lambda (filepath)
                   (cond
      		;; 1. 파일 존재 여부 확인
      		((not (file-exists-p filepath))
      		 (format "Error: 파일을 찾을 수 없습니다: %s" filepath))
      		
      		;; 2. 파이썬 명령어 확인
      		((not (executable-find "python3"))
      		 "Error: 시스템에 'python3' 명령어가 없습니다.")
      		
      		;; 3. Python 인라인 스크립트를 통한 텍스트 추출
      		(t
      		 (let* ((py-script "
      import sys
      try:
          from pypdf import PdfReader
          reader = PdfReader(sys.argv[1])
          text = '\\n'.join([page.extract_text() for page in reader.pages if page.extract_text()])
          print(text)
      except ImportError:
          print('Error: pypdf 라이브러리가 설치되지 않았습니다. 터미널에서 pip3 install pypdf 를 실행하세요.')
      except Exception as e:
          print(f'Error: PDF를 읽는 중 오류가 발생했습니다 ({str(e)})')
      ")
      			;; 쉘 명령어 실행 (파이썬 스크립트에 filepath 전달)
      			(command (format "python3 -c %s %s" 
      					 (shell-quote-argument py-script) 
      					 (shell-quote-argument filepath)))
      			(text (shell-command-to-string command)))
                       
                       (if (string-empty-p (string-trim text))
                           "Error: 문서에서 텍스트를 추출할 수 없습니다. (스캔된 이미지이거나 암호가 걸려있을 수 있습니다.)"
                         text))))))

      )
#+end_src

** GPTEL-AGENT

#+begin_src emacs-lisp

  (use-package gptel-agent
    :ensure (:host github :repo "karthink/gptel-agent" :files ("*.el" "agents"))
    :after gptel
    :config
    (add-to-list 'gptel-agent-dirs (expand-file-name "~/project/ai/gptel-agents/"))
    (unless (file-exists-p "~/project/ai/gptel-agents/")
      (make-directory "~/project/ai/gptel-agents/" t)))
#+end_src

** SDD

#+begin_src emacs-lisp
  (defun sdd--create-file-with-template (dir filename content)
    "주어진 디렉토리(DIR)에 파일(FILENAME)을 생성하고 내용(CONTENT)을 삽입합니다."
    (let ((filepath (expand-file-name filename dir)))
      (with-temp-file filepath
        (insert content))))

  (defun sdd-new-iteration (base-dir iter-name)
    "Spec Driven Development를 위한 새로운 이터레이션 디렉토리와 Org 파일들을 생성합니다."
    ;; 사용자가 디렉토리와 이터레이션 이름을 순서대로 입력하도록 변경
    (interactive
     (list (read-directory-name "Project Directory: " default-directory)
           (read-string "Iteration Name (예: 001-multi-objects): ")))
    
    (let* ((dir-name (format "iter-%s" iter-name))
           ;; 선택한 base-dir 하위에 새로운 이터레이션 디렉토리 경로 지정
           (dir-path (expand-file-name dir-name base-dir)))
      
      (if (file-exists-p dir-path)
          (message "디렉토리 %s 가 이미 존재합니다!" dir-path)
        (progn
          (make-directory dir-path t)
          
          ;; 1. context.org
          (sdd--create-file-with-template
           dir-path "context.org"
           "* Current State\n\n* Objective\n\n* Technical Context\n\n* Prompt Template :noexport:\n#+BEGIN_QUOTE\n너는 ClojureScript와 Pixi.js를 전문으로 다루는 시니어 웹 게임 아키텍트야.\n아래 현재까지 구현된 코드베이스 상태와 이번 이터레이션 목표를 바탕으로,\n다음 단계에서 로컬 LLM이 완벽하게 이해하고 코딩할 수 있도록 context.org 파일에 들어갈 내용을 작성해줘.\n\n작성 규칙:\n1. Emacs Org-mode 포맷을 엄격히 준수할 것.\n2. ClojureScript의 함수형 패러다임(atom)과 Pixi.js의 객체지향적 렌더링을 어떻게 연결할지 명확한 가이드를 포함할 것.\n\n[현재 상태 및 코드 요약]\n\n[이번 이터레이션 목표]\n#+END_QUOTE\n")
          
          ;; 2. todo.org
          (sdd--create-file-with-template
           dir-path "todo.org"
           "* Tasks\n\n* Test Cases\n\n* Prompt Template :noexport:\n#+BEGIN_QUOTE\n앞서 작성된 context.org의 내용을 바탕으로 실제 개발을 진행할 todo.org 문서를 작성해줘. \n이 문서는 파라미터 수가 적은 로컬 LLM이 읽고 하나씩 구현할 것이므로, 지시사항이 매우 구체적이고 원자적(Atomic)이어야 해.\n\n작성 규칙:\n1. Org-mode의 체크박스(- [ ]) 형태를 사용할 것.\n2. 파일명별로 작업을 나누고, 함수 입출력 형태를 명시할 것.\n3. Test Cases 섹션을 만들고, Cider REPL에서 평가해 볼 수 있는 코드를 적어줄 것.\n\n[context.org 내용]\n#+END_QUOTE\n")
          
          ;; 3. worklist.org
          (sdd--create-file-with-template
           dir-path "worklist.org"
           "* Work Completed\n\n* Prompt Template :noexport:\n#+BEGIN_QUOTE\n너는 ClojureScript 코더야. 불필요한 인사말이나 장황한 설명은 생략하고, 오직 요구받은 코드와 작업 요약만 출력해.\n아래 todo.org의 작업 중 [수행할 작업 번호]를 구현해줘.\n\n작성 규칙:\n1. 기존 코드를 수정하는 경우, 전체를 다시 쓰지 말고 수정되는 함수만 제공할 것.\n2. 코드 작성이 끝나면, 네가 어떤 작업을 수행했는지 짧게 요약해 줄 것.\n\n[현재 파일 코드]\n\n[수행할 작업]\n#+END_QUOTE\n")
          
          ;; 4. tests.org
          (sdd--create-file-with-template
           dir-path "tests.org"
           "* Test Results\n\n* Prompt Template :noexport:\n#+BEGIN_QUOTE\n코드를 작성하고 Cider REPL에서 테스트를 하던 중 아래와 같은 에러(또는 원치 않는 결과)가 발생했어.\n원인을 분석하고 해결하기 위한 ClojureScript 수정 코드를 제안해줘.\n\n작성 규칙:\n해결책을 제시한 후, 이 이슈를 tests.org에 기록할 수 있도록 문제 원인과 해결 방법을 짧은 요약으로 제공해.\n\n[발생한 문제 / 에러 스택 트레이스]\n\n[문제가 발생한 코드 부분]\n#+END_QUOTE\n")
          
          ;; 5. validation.org
          (sdd--create-file-with-template
           dir-path "validation.org"
           "* Validation Summary\n\n* Next Steps\n\n* Prompt Template :noexport:\n#+BEGIN_QUOTE\n이번 이터레이션의 목표에 대한 작업과 테스트가 모두 끝났어.\n아래의 worklist.org와 tests.org 내용을 분석해서, 이번 이터레이션을 마무리하는 validation.org 문서를 작성해줘.\n\n작성 규칙:\n1. 이번 이터레이션에서 달성된 기술적 포인트들을 요약할 것.\n2. 남아있는 기술적 부채나 개선 포인트가 있다면 짚어줄 것.\n3. 다음 이터레이션의 목표와 예상 작업 아이디어를 제안할 것.\n\n[worklist.org 내용]\n\n[tests.org 내용]\n#+END_QUOTE\n")
          
          ;; 작업 완료 후 context.org 파일을 엽니다.
          (find-file (expand-file-name "context.org" dir-path))
          (message "성공적으로 %s 에 이터레이션을 생성했습니다." dir-path)))))
#+end_src


** 저사양기기 

#+begin_src emacs-lisp

  (setq-default
   ;; eldoc이 너무 자주 호출되어 CPU를 점유하는 것을 방지
   eldoc-idle-delay 0.5
   
   ;; eglot 관련 이벤트 로그 비활성화 (디버깅 시에만 켬)
   eglot-events-buffer-size 0
   
   ;; corfu의 자동 완성 지연 시간을 살짝 늘려 타이핑 부하 감소
   corfu-auto-delay 0.2
   corfu-auto-prefix 2)
#+end_src
